extends ../layout

//- 프로젝트 관리 페이지
//- 검색 전 보여지는 값은 현재 날짜에 진행중인 프로젝트
block content
  h1.text-center.mt-3 프로젝트 관리
  button.btn.btn-outline-primary(type="button" onclick="location.href='addProject'") 추가
  table.table.text-center
    thead
      tr
        th(scope="col") num
        th(scope="col") project_name
        th(scope="col") begin_date
        th(scope="col") end_date
        th(scope="col") order_customer
    tbody
      each value in result
        tr
          td= value[0]
          td= value[1]
          td= moment(value[2]).format('YYYY-MM-DD')
          td= moment(value[3]).format('YYYY-MM-DD')
          td= value[4]