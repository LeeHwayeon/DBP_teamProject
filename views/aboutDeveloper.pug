extends layout

//- 직원 관리 페이지(경영진)
block content
    //- 직원 검색
    h2.text-center.mt-3 직원 검색
    form(action='/showDeveloper', method='post')
      .input-group.mb-3
        input.form-control(type="text" name="DeveloperId" placeholder="input Developer Id!")
        .input-group-append
          button.btn.btn-outline-secondary(type="submit") 검색
    //- 검색 전 모든 직원 정보 보여줌, 검색 후 검색된 직원 정보만 보여줌
    h4 직원 정보
    form(action='/aboutDeveloperDetail', method='post')
        table.table.text-center(width="700" border="1")
            thead
                tr
                    th(scope="col")                 
                    th(scope="col") Id
                    th(scope="col") 이름
                    th(scope="col") 경력
            tbody
                each d in result
                    tr
                        td
                            .form-check
                                input#defaultRadio1.form-radio-input(type="radio" name="developer" value=`${d[0]}`)
                        td=d[0]
                        td=d[1]
                        td #{moment.duration(moment(new Date()).diff(d[2])).asMonths().toFixed(2)}개월
        .row.justify-content-start.mt-4
            .col(style="margin-bottom: 1em")
                button.btn.btn-dark.mr-3(type="submit", style="float: right") 상세 정보 조회